% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_config.R
\name{as_filters}
\alias{as_filters}
\title{Configure the Filters for a Data Frame}
\usage{
as_filters(data, ..., ns = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{...}{Named arguments passed to \code{\link[=filterInput]{filterInput()}} for every column, such
as \code{slider = TRUE} or \code{selectize = TRUE}.}

\item{ns}{An optional namespace created by \code{\link[shiny:NS]{shiny::NS()}}.}
}
\value{
A \code{shinyfilters} object:
\itemize{
\item \code{names(filters)} lists its columns.
\item \code{filters$col} and \code{filters[["col"]]} return the input \code{\link[=filterInput]{filterInput()}}
creates for one column, including any \code{\link[=with_filter]{with_filter()}} overrides.
\item \code{filters[cols]} returns a \code{shinyfilters} object with only the selected
columns, keeping their overrides. \code{cols} uses
<\code{\link[tidyselect:language]{tidy-select}}>, like \code{\link[=with_filter]{with_filter()}}; use
\code{all_of()} to select with a variable.
}
}
\description{
\code{as_filters()} stores a data frame with the arguments used to create its
filters. Pass the result to \code{\link[=with_filter]{with_filter()}} to choose the input for
individual columns, then to \code{\link[=filterInput]{filterInput()}} to create the inputs.
}
\examples{
filters <- as_filters(nyc_flights)
filters

# Use sliders for every numeric column
filters <- as_filters(nyc_flights, slider = TRUE)
filterInput(filters)

# The input for one column
filters$origin
}
\seealso{
\code{\link[=with_filter]{with_filter()}}
}
